{{- if and .Values.rbac.create (eq .Values.rbac.useClusterRole false) -}}
{{ range $.Values.namespaces -}}
---
apiVersion: {{ template "rbac.apiVersion" $ }}
kind: RoleBinding
metadata:
  labels:
    {{- include "my-images.labels" $ | nindent 4 }}
  name: {{ template "my-images.fullname" $ }}
  namespace: {{ . }}
subjects:
  - kind: ServiceAccount
    name: {{ template "my-images.serviceAccountName" $ }}
{{ include "my-images.namespace" $ | indent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
{{- if (not $.Values.useExistingRole) }}
  name: {{ template "my-images.fullname" $ }}
{{- else }}
  name: {{ $.Values.useExistingRole }}
{{- end }}
{{- end }}
{{ end }}